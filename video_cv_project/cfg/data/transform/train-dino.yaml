# Config file for train pipeline for DINO ViT model.

defaults:
  # Base pipeline config.
  - base

im_transforms:
  - _target_: video_cv_project.data.transform.HFTransform
    name: /home/jovyan/workspaces/video-cv-project/pretrained/facebook/dino-vits8
    #name: /home/jovyan/workspaces/video-cv-project/pretrained/facebook/vit-msn-small
    #do_resize: false
    # TODO: If below is list or dict, have to convert from OmegaConf to native.
    size: 384

pipeline:
  # Normalization already done in HFTransform.
  do_rgb_norm: false
