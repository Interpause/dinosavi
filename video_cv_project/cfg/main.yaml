# Main config file for video_cv_project.

defaults:
  - _self_
  - hydra: custom
  - transform: base
  - model: crw
  - mode: help

exp_name: exp

# Path (absolute or relative) to the model.
# For example: outputs/exp_2020-01-01_00-00-00/weights/latest.ckpt
resume:
device: cuda

verbose: [video_cv_project]
