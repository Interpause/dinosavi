# @package _global_
# Config for eval mode for SlotModel.

defaults:
  - slot-base
  - override /model: slot
  - override /data: davis

mode: eval-slot

data:
  dataset:
    context_len: 0

  transform:
    im_transforms:
      - _target_: video_cv_project.data.transform.HFTransform
        name: ${vit_path}
        do_resize: false
        size:
          height: 360
          width: 480
